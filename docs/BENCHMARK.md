# Benchmark Methodology

The repository benchmark is a conformance suite, not a claim that any model provider is universally better. Fixtures isolate operational failure modes and contain no real customer data.

Run:

```bash
npm run benchmark
```

The command writes `benchmark-results/latest.json`. Every benchmark result can be reproduced from public fixtures and evaluator code.
